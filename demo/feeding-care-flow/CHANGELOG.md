# Changelog

## 0.5.18 - 2026-09-22

- 移除持续漏气弹窗标题上方的“强提醒 · 已停止输出”标签，让用户直接阅读异常标题和处理动作。
- 压力过高弹窗中的“安全保护 · 已停止输出”保持不变。

## 0.5.17 - 2026-09-22

- 从压力过高弹窗中移除“未确认的最佳吸力不会保存”及候选档位、稳定计时说明。
- 弹窗仅保留压力异常原因、重新佩戴与连接检查、重新检测和结束吸乳操作。

## 0.5.16 - 2026-09-22

- 将持续漏气弹窗标题改为“检测到左/右侧持续漏气，请重新佩戴”，让异常原因与当前任务合并展示。
- 将主操作改为“已重新佩戴，继续吸乳”，并说明点击后会重新启动吸乳、检测佩戴状态。
- 持续漏气状态统一为暂停输出并等待重新佩戴；压力过高仍保留安全终止语义。

## 0.5.15 - 2026-09-22

- 将法兰指引第三页 CTA 改为“开始吸乳并检测佩戴”，明确点击后会立即启动吸乳。
- 在 CTA 上方补充流程说明，告知用户将依次完成佩戴检测、最佳吸力设置，并在完成后继续吸乳。

## 0.5.14 - 2026-09-22

- 将右侧评审面板标题由 `V1.1 流程控制` 简化为 `流程控制`。
- 在面板顶部增加三条简短使用说明，帮助评审者按场景、检测结果和运行中状态顺序操作。

## 0.5.13 - 2026-09-22

- 将 Standard mode 主控制卡和模式选择弹层中的 `Stimulate` 展示文案统一改为 `Stimulation`。
- 内部模式值和现有交互逻辑保持不变。

## 0.5.12 - 2026-09-22

- 从模式 List 页的 Manual 区域移除 `Mixed` 卡片，仅保留 `Stimulation` 和 `Expression`。
- 旧缓存如曾选中 Manual Mixed，会自动恢复为 `Stimulation`。
- Programs 中原有的 `Mixing` 节奏段保持不变。

## 0.5.11 - 2026-09-22

- Standard mode 继续只显示 `Stimulate` 和 `Expression`，但恢复原四选项布局中前两个按钮的位置与间距。
- 保留后两个网格位置为空，不再把前两个按钮拉宽或重新分布。

## 0.5.10 - 2026-09-22

- 缩短 Standard mode 两个模式按钮的横向占用区域，使 `Stimulate` 与 `Expression` 紧邻排列。
- 保留按钮、图标和文字的原有尺寸，不再让两个选项分散占满整张模式卡片。

## 0.5.9 - 2026-09-22

- 在 `Stimulate` 模式的 Level 区域同步展示“最佳吸力”入口，交互与 `Expression` 保持一致。
- 从最佳吸力设置页保存或取消后保留进入前的 Standard mode，不再强制切换为 `Expression`。

## 0.5.8 - 2026-09-22

- 从 `Standard mode` 主控制卡中移除多余的 `Mixed` 和 `Milk initiation`，仅保留 `Stimulate` 与 `Expression`。
- 同步精简模式选择弹层，并将主控制卡调整为两列布局。
- 旧缓存如保存了已移除模式，会自动恢复为 `Stimulation`。

## 0.5.7 - 2026-09-22

- 设备教学完成页点击 `Start using V4` 后，直接在吸乳控制页自动弹出首次使用 Workflow。
- 首次使用 Workflow 不再显示关闭符号，并移除弹窗背后的返回热区，用户必须走完首次流程。
- 保留设备页、洞察底部控制条等其他控制页入口的原有跳转行为，不重复触发首次流程。

## 0.5.6 - 2026-09-22

- 将原本位于左侧目录底部的 `V1.1 流程控制` 移到手机 Demo 右侧，作为常驻评审控制区。
- 保留首次使用、日常吸乳、检测结果、运行中异常、设备断连和最佳吸力候选的现有状态逻辑。
- 窄屏下控制区自动排列到手机下方，避免遮挡或挤压正式产品界面。

## 0.5.5 - 2026-09-22

- 从第 2 步佩戴检测开始显示 Hold to Finish 与暂停按钮，表示设备已进入实际吸乳输出阶段。
- 暂停吸乳时同步暂停佩戴检测倒计时；关闭准备流程时停止本次吸乳输出。

## 0.5.4 - 2026-09-22

- 将运行中 Workflow、佩戴状态和吸乳时长移入页面滚动画布，修复向下滑动后产品图与 Workflow 错位、重复露出的问题。
- 保持底部 Hold to Finish 与暂停按钮固定，不影响结束吸乳操作。

## 0.5.3 - 2026-09-22

- 日常“再次吸乳”的 Workflow 页面复用“继续吸乳”运行页内容，仅保留 Standard mode、Level 和 Light，移除 Frequency 区域。

## 0.5.2 - 2026-09-22

- 将 Workflow 下方的吸乳时长下移，避免与 Workflow 卡片重叠，同时保留与 Standard mode 卡片的间距。

## 0.5.1 - 2026-09-22

- 将完整 Demo 的法兰指引、佩戴检测、最佳吸力、继续吸乳和吸乳中 Workflow 直接对齐线上独立流程 Demo `67636a7`。
- 更新 CSS 与 JavaScript 资源版本号，避免浏览器继续使用 `0.5.0` 的旧 Workflow 缓存。

## 0.5.0 - 2026-09-21

- 将院端与居家端连接后的吸乳准备流程统一升级为 V1.1：`法兰指引 → 佩戴检测 → 最佳吸力 → 继续吸乳`，保留原有 31 个页面及后续记录、洞察、设备和社区流程。
- 首次使用支持三页法兰指引、真实 5 秒左右侧佩戴检测、异常后完整重检、1–15 档最佳吸力确认保存，以及实际执行的 `3 → 2 → 1` 继续吸乳倒计时；日常吸乳自动确认法兰方案并应用已保存的最佳吸力。
- 吸乳中增加常驻 Workflow 与左右佩戴状态，支持弱提醒继续吸乳、持续严重漏气立即终止、压力过高独立安全终止、连接中断后结果失效，以及终止后重新完成 5 秒检测。
- 运行中调档只立即作用于本次吸乳，不直接覆盖最佳吸力；稳定使用 60 秒后可选择更新为新档位或仅本次使用，暂停、佩戴异常、严重异常、断连和切出 Expression 模式会暂停、重置或取消候选。
- 左侧 Demo 目录增加 V1.1 评审控制，可切换首次/日常场景、预设检测结果、触发异常与断连、模拟设备指令失败，并快进候选档位计时。

## 0.4.27 - 2026-08-31

- 移除院端和居家端吸乳子首页顶部独立的 Daily summary 卡片，将其标题和总结文案合并到 Pumping Trend 卡片底部。
- 在 Pumping Trend 总结区增加 CozyAI 入口并自动携带 Trend 洞察上下文，同时保留 `More` 详情入口。
- 保持 Lactation、Expert guidance、Pumping moms 和底部设备控制条的现有内容与交互不变。

## 0.4.26 - 2026-08-31

- 将院端和居家端共享的 CozyAI Device 页面同步到 Figma 新节点 `961:55521`：采用粉白渐变背景、完整粉色兔子角色、`Momcozy Air 1` 标题与 Beta 标识。
- 按参考稿更新三条推荐问题、圆形返回按钮和底部输入区的尺寸、间距、文案与图标，并将 Figma 临时资源本地化到版本化资源文件。
- 保留现有推荐问题、自定义输入、即时回复、洞察上下文和真实访问历史返回逻辑，双端继续使用独立状态。

## 0.4.25 - 2026-08-31

- 将院端和居家端 Pumping 吸乳数据页左上角返回固定改为进入设备页，不再使用不确定的历史返回目标。
- 将共享页面目录及相关入口中的 `AI 设备页`、`AI 吸乳子首页` 分别更名为 `设备页`、`吸乳子首页`。

## 0.4.24 - 2026-08-28

- 修正院端和居家端控制页最佳档位测试入口的名称，完成测试后仍统一显示 `Best level test`，不再错误显示 `Retest best level`。
- 保持 `Expression` 模式下的入口位置、测试结果回填和再次进入测试的交互逻辑不变。

## 0.4.23 - 2026-08-28

- 修正院端和居家端 AI 吸乳子首页底部设备控制条的设备缩略图，改用 Figma 参考截图中的 V4 控制器实物图，不再显示上一版误用的 V3 设备素材。
- 保持 Figma 节点 `817:50147` 的 54px 设备外框与 52.8×39px 图像几何，以及原有渐变、双行结构、播放胶囊和 Home Indicator 不变。

## 0.4.22 - 2026-08-28

- 将院端和居家端共享的 AI 吸乳子首页底部设备控制条对齐 Figma 节点 `817:50147`：采用酒红至粉红渐变、设备与 `Mobile flow` 顶部行、细分隔线及 `Milk Boost` 会话底部行。
- 播放入口改为 88×56 横向胶囊按钮，并补齐设备切换、进入箭头和白色 Home Indicator；控制条固定高度调整为 178px。
- 扩大数据内容底部安全区，保证 402×874 视口下 Lactation、Expert guidance 与 Pumping moms 内容可完整滚动查看，不被固定控制条遮挡。

## 0.4.21 - 2026-08-27

- 将院端和居家端 AI 设备页的 `Breast Pump` 设备卡点击目标改为 `AI 吸乳子首页`。
- 保持设备卡原有尺寸和视觉不变，不恢复已从 AI 设备页移除的洞察及社区模块。

## 0.4.20 - 2026-08-27

- 从院端和居家端共享的 AI 设备页移除 `AI pumping insights` 与 `Pumping moms` 两个叠加模块。
- 同步移除对应 CozyAI、View more 和社区点击入口及未再使用的页面样式，保留设备卡和底部导航。

## 0.4.19 - 2026-08-27

- 将院端和居家端控制页、自动检测页及吸乳中页面左上角返回统一改为进入 `AI 吸乳子首页`。
- 检测弹层打开时返回热区保持可点击，返回后取消当前检测自动跳转；其余页面继续使用真实访问历史回退。

## 0.4.18 - 2026-08-27

- 将院端和居家端佩戴检测通过页的自动停留时间由 3 秒缩短为 1 秒。
- 保持检测完成后自动显示开始吸乳 toast 并进入吸乳中，无需用户再次点击按钮。

## 0.4.17 - 2026-08-27

- 院端和居家端仅保留设备连接流程差异；从设备教学开始共用同一套页面定义、目录结构和交互跳转。
- 将院端连接后的控制、模式、检查、吸乳、记录、数据洞察、设备、CozyAI 与社区逻辑完整同步到居家端。
- 所有动态 CozyAI 入口统一使用 `V4 Setup` Step 2 的粉色兔子 logo；Figma 原始截图资产保持不变。

## 0.4.16 - 2026-08-27

- 院端和居家端佩戴检测通过页改为停留 3 秒后显示开始吸乳 toast，并自动进入各自的吸乳中页面。
- 移除检测通过页的 `Start pumping` 按钮，避免用户在自动检查完成后再次确认。

## 0.4.15 - 2026-08-27

- 为院端和居家端 `V4 Setup` Step 1 顶部右侧增加与 Step 2 一致的 CozyAI 图标入口。
- Step 1 复用现有 CozyAI 对话和真实历史返回逻辑，Step 2 的原截图与交互保持不变。

## 0.4.14 - 2026-08-27

- 将院端和居家端 List 左上角 `×` 改为固定关闭模式列表并返回各自控制页，不再受模式详情或自定义编辑访问历史影响。
- 返回控制页时同步清理本次模式流程历史，避免控制页的后续返回再次进入 List。

## 0.4.13 - 2026-08-27

- 为 List 中三种 Manual 模式和两个 Programs 卡片正文增加切换二次确认窗口，明确展示即将应用的目标模式。
- `Keep current` 和遮罩点击会留在 List 并保留当前设置；只有点击 `Switch` 才应用目标模式并返回对应控制页，`Details` 入口不受影响。

## 0.4.12 - 2026-08-27

- 修正院端和居家端自定义分段模式的保存跳转：Edit 页面点击 `Save` 后返回独立 `List`，不再直接进入控制页。
- 保存后的自定义模式名称、分段参数和已保存状态继续保留，双端仍使用各自独立的流程与本地状态。

## 0.4.11 - 2026-08-27

- 将院端和居家端控制页及吸乳中模式弹层的手动模式名称由 `Lactation` 统一为 `Expression`，与独立 List 页面保持一致。
- 同步更新 `Best level test` 的显示、进入和结果回填判断；数据分析中的 Lactation 指标与图表保持不变。

## 0.4.10 - 2026-08-27

- 将院端和居家端控制页右上角设置改为进入独立的设备管理页，按参考图展示 V4 在线状态、设备名称、SN、MAC、当前版本、更新和恢复出厂设置。
- 补齐信息复制、检查更新、恢复默认与删除设备反馈，并保持设备管理页按真实历史返回；吸乳中的设置仍保留原有弹层。

## 0.4.9 - 2026-08-27

- 将院端和居家端控制页右上角的问号改为进入各自的 `V4 Setup` 设备助手页，关闭时按真实访问历史返回控制页。
- 吸乳中的问号继续保留原有 `Pumping help` 弹层，避免改变进行中场景的帮助交互。

## 0.4.8 - 2026-08-27

- 将院端和居家端控制页的 `Switch` 统一改为进入 Figma 风格的独立 `List` 页面，不再打开 `Switch mode` 底部弹层。
- 模式列表、Manual 应用、Programs 详情和自定义模式流程均按当前入口返回对应控制页，继续保持双端历史与本地状态独立。

## 0.4.7 - 2026-08-27

- 为院端和居家端的奶量记录页增加左右独立的纵向滑动控制，滑动时通过球体内液位反馈实时显示调整结果。
- 左右奶量数值改为可编辑输入框，支持键盘输入、回车确认、失焦校正及 `0–300 ml` 边界限制，并与滑动和原有上下微调双向同步。

## 0.4.6 - 2026-08-27

- 修正 Standard mode 的信息层级：切换 Stimulation、Lactation、Mixed 或 Milk initiation 时，顶部标题和说明保持 `Standard mode`，不再改写为具体子模式名称。
- Lactation 仍保留图标选中态和 `Best level test` 入口；院端、居家端及吸乳中共用逻辑同步生效。

## 0.4.5 - 2026-08-27

- 将已确认的新版吸乳洞察页面同步到院端和居家端：Daily summary 保留唯一 CozyAI 主入口，Pumping Trend 与 Lactation 只显示 `Key insight + More`。
- 新增两类洞察详情和独立 CozyAI Device / Beta 对话页；支持分析上下文、推荐问题、自定义输入、即时回复，并按真实访问历史返回。
- 新增 Expert guidance、84px Pumping moms 紧凑入口和 118px 固定酒红设备控制条，同时采用 Figma 的背景、字体层级、22px 圆角、细边框与轻阴影语言。
- 全局移除主控制、吸乳中和自定义分段中的 Frequency；主控、最佳档位测试和分段编辑统一使用 1–15 档。
- 院端与居家端均完成 402×874 视觉、滚动、交互、返回和控制上限验证，继续保持独立入口与独立本地状态。

## 0.4.4 - 2026-08-26

- 居家端 `Programs` 卡片的 `Details` 入口新增独立韵律详情页，按 Figma 节点 `934:50097` 展示六段 Sequential execution 时间线。
- 详情页返回到真实来源 List；预设模式卡片正文继续支持直接应用，Manual 三种模式仍保持点击即应用且不弹出 Rhythm 选择弹窗。
- 合并并保留 0.4.3 的最新 Figma 截图资产、教学流程、控制页和最佳档位交互更新。

## 0.4.3 - 2026-08-25

- 同步 Figma 最新的院端发现/扫描页，以及两端共用的 V4 教学欢迎页、Step 1、Step 2 和教学完成页；旧版 PNG 全部保留用于回滚。
- 控制页同步 Lactation mode 标题、说明、Best level test 和新版 Switch 样式，并修复夜灯关闭后动态控件透出底图造成的文字与开关重影。
- 佩戴通过页移除多余提示卡，最佳档位结果更新为 `BEST LEVEL SET` 与 `Return to control`；教学最后页的 Cozy 图标接入 AI 助手并支持按真实历史返回。
- 居家端手动模式改为点击即应用并直接返回吸乳控制页，不再打开 `Rhythm` 韵律弹窗。
- 从居家端左侧页面导航中移除韵律选择弹窗，院端流程保持不变。

## 0.4.2 - 2026-08-24

- 补齐院端与居家端所有页面中明确可操作元素：设备卡、扫码/刷新/闪光灯、验证码键盘、教学视频与帮助项、完成页奶量和时长、底部导航及辅助入口。
- 吸乳中页面继续支持模式、档位、频率、灯光、帮助与设置调整；社区加入、当前状态分享和发帖入口改为可见的持久交互状态。
- 两个场景统一交互规则与反馈样式，同时继续保留独立入口、独立访问历史和独立 `localStorage`。

## 0.4.1 - 2026-08-24

- 院端和居家端控制页补齐帮助、设置、模式切换、档位、频率、灯光与开始吸乳交互；所有选择均有即时视觉反馈并写入各自独立状态。

## 0.4.0 - 2026-08-24

- 院端和居家端设备页新增一行 AI 吸乳数据总结、Cozy AI 图标入口和 `Pumping moms` 社区群组入口。
- `View more` 进入新版 AI 吸乳子首页；三个数据图卡均补充详细 `AI Insight` 与可跳转的 `More`。
- 新增 AI 洞察详情页和社区群组页，返回、加入群组与分享状态交互均可用。
- 保留 0.3.22 的设备页、子首页 PNG 和显式回滚引用，新页面使用独立渲染结构与存储版本。

## 0.3.22 - 2026-08-24

- 删除 `Start Pumping` 后重复的穿戴提示页，点击后直接自动进入泌乳启动和佩戴检测。
- 开始前检查从三步缩减为两步，佩戴通过后直接开始吸乳，舒适档位不再是必经流程。
- 院端和居家控制页补齐模式切换和预设档位交互；只有选中 `Lactation` 时才在档位旁显示 `Best level test` 入口。
- 最佳档位测试改为可选分支，确认结果后回到控制页，并将档位带入后续吸乳页。

## 0.3.21 - 2026-08-21

- 按新版参考图重做居家模式列表：恢复深灰状态栏、圆角白色内容层、`Manual / Programs` 分组和两张完整节奏卡。
- Manual 三种模式统一接入底部 `Rhythm` 韵律弹窗，支持 Gentle、Balanced、Intense 三档韵律选择。
- 韵律弹窗支持关闭、返回、切换和应用，应用结果会回填到居家吸乳控制页。
- 新增韵律弹窗左侧导航页并保持院端 Demo 不变。

## 0.3.20 - 2026-08-21

- 在居家吸乳控制页的 `Switch` 入口后新增完整模式管理分支。
- 新增模式列表、新模式命名、分段总览、参数编辑、完整预览和模式说明 6 个可交互页面。
- 支持两段模式的类型、档位、频率、灯效、开关和试听交互，并将保存结果带回吸乳控制页。
- 新页面全部加入左侧目录和历史返回栈，保持院端 Demo 流程不变。

## 0.3.19 - 2026-08-21

- 为院端和居家端所有可见返回按钮补齐交互热区。
- 返回操作优先回到实际访问的上一个页面，无页面历史时按 Demo 流程顺序回退。
- 返回绑定中、连接中和记录成功等自动跳转页时暂停自动前进，避免刚返回就再次跳走。
- 返回页面时同步恢复绑定、教学、吸乳和记录状态。
- 院端与居家端状态均按版本隔离，避免旧版本标签页覆盖当前 Demo 的返回结果。

## 0.3.18 - 2026-08-21

- 院端和居家端进入吸乳中页面时，档位默认沿用前一步确认的舒适负压档位。
- 动态替换吸乳中截图内写死的 `Level 1` 和 `1 / 12`，保证上下两处数值一致。
- 吸乳中的 `− / +` 支持在 1–12 档之间继续调节。

## 0.3.17 - 2026-08-21

- 移除根入口中院端和居家端并排展示的 iframe 组合页。
- 院端与居家端只保留各自独立的 Demo 页面、流程状态和访问地址。
- 根目录地址改为直接跳转院端 Demo，避免再次把两个 Demo 放在同一个页面中。

## 0.3.16 - 2026-08-21

- 院端和居家端在 `Start Pumping` 后新增穿戴提示弹框，确认佩戴完成后再进入泌乳启动。
- 穿戴提示首次打开时，整张弹框从手机底部上滑，遮罩同步淡入。
- 检测步骤内部切换改为轻量内容过渡，调节舒适负压时不会重复触发整张弹框动画。
- 弹框打开期间锁定背景滚动，并补充底部弹层拖拽提示、对话框语义和减少动态效果支持。

## 0.3.15 - 2026-08-20

- 院端和居家端在开始吸乳前新增统一的 5 页设备检查流程。
- 所有检查页共用顶部三步进度：数字在上、文字在下，进行中为红色、已通过为绿色、未开始为灰色。
- 串联泌乳启动、佩戴检测、检测通过、舒适负压搜索和舒适度确认，并支持调节负压、重新测试和开始吸乳。
- 两个 Demo 的左侧目录同步加入检查页，仍保持独立状态与独立运行。

## 0.3.14 - 2026-08-20

- 院端 Demo 从 16 页扩展为 17 页，末尾新增“设备子首页”。
- “吸乳子场景卡”左上角返回按钮现在进入“设备子首页”。
- 保持“记录成功”自动跳转到“吸乳子场景卡”，不会跳过第 16 页。

## 0.3.13 - 2026-08-20

- 用 Figma 节点 `833:44820` 的最新整帧替换设备教学欢迎页，标题更新为 `Welcome to V4`。
- 按 Figma 节点 `833:46196` 重做吸乳中底部控件：粉色 `Hold to Finish` 与独立暂停按钮。
- 暂停按钮支持暂停/继续状态切换，不影响 1.2 秒长按结束逻辑。
- 子首页 `Mobile flow` 缩略图替换为 V4 设备图，院端和居家端共用页面同步更新。

## 0.3.12 - 2026-08-20

- 遮掉吸乳控制页整页截图中原有的 `Start Pumping` 按钮，解决滚动到底部时与悬浮按钮重叠的问题。
- 移除原按钮的透明点击热区，两端均只保留一个真实可交互的悬浮按钮。

## 0.3.11 - 2026-08-20

- 院端和居家端的吸乳控制页新增固定悬浮 `Start Pumping` 按钮。
- 按钮保持在手机视口底部最上层，控制页滚动时仍然可见可点击。
- 保留院端和居家端各自的开始吸乳流程。

## 0.3.10 - 2026-08-20

- 从 Figma 节点 `833:47304` 下载原始设备 PNG，补齐吸乳器控制页缺失的设备图。
- 按 Figma 的 `182 × 138` 尺寸及 `left: 89px; top: 108px` 位置等比覆盖。
- 院端和居家端复用同一控制页资产，因此两个 Demo 同步修复。

## 0.3.9 - 2026-08-20

- 院端和居家端独立 Demo 新增左侧 `Demo 目录`，支持点击任意页面直接跳转。
- 目录按设备连接、设备教学、吸乳与记录分组，并高亮当前页面。
- 目录顶部增加院端/居家端快速切换，底部保留当前流程重置入口。
- 桌面端常驻左侧，窄屏自动切换为可展开的侧边抽屉。

## 0.3.8 - 2026-08-20

- 修复设备图覆盖层只在居家子首页生效的问题。
- 改为按 `home-07-dashboard.png` 整屏资产匹配，院端和居家端复用页面均显示新设备图。
- 更新三个入口的缓存版本，避免旧标签页继续加载无覆盖层的脚本。

## 0.3.7 - 2026-08-20

- 使用 Figma `833:46216` 中的原始 V3 设备 PNG 覆盖子首页底部设备缩略图。
- 保留 `138 × 104` 源图比例，并按 Figma 的 `54 × 54` 外框和 `52.8 × 39` 可见尺寸定位。
- 覆盖层保持透明且不接收点击，不影响 `Mobile flow` 卡片原有交互。

## 0.3.6 - 2026-08-20

- 按 Figma 第二页重做居家端“设备已添加”页面。
- 使用完整 393 × 852 设计截图，恢复吸乳器图标、右箭头、兔子装饰和准确卡片排版。
- 移除旧的临时叠加卡片，仅保留 `Get Start` 等比交互热区。
- 为三个入口的脚本和样式增加版本参数，避免旧缓存继续显示上一版卡片。

## 0.3.5 - 2026-08-19

- 在居家独立 Demo 原流程前增加设备空态、发现 V4、连接中、连接完成和设备已添加步骤。
- 居家端点击 `Get Start` 后才进入原有设备教学、控制、吸乳和记录流程。
- 连接弹窗复用 Figma 设备空态原图并叠加独立状态，设备图使用 Figma 导出资产。
- 所有居家步骤改用节点 ID 查找，避免前置步骤导致后续流程索引错位。
- 居家端启用新的本地状态版本，避免旧版进度跳过新增连接流程。
- 院端流程和存储状态保持不变。

## 0.3.4 - 2026-08-19

- 院端和居家端统一使用 iPhone 17 的 `402 × 874` 逻辑视口。
- 较长的控制页面改为在手机视口内部滚动，外部设备尺寸保持稳定。
- 院端和居家端的吸乳中页面增加 1.2 秒 `Hold to finish` 长按结束按钮。
- 移除点击设备图片直接结束吸乳的旧热区。

## 0.3.3 - 2026-08-19

- 按 Figma 顺序继续扩展院端：控制页、吸乳中、完成吸乳、奶量记录、Logged 和吸乳子场景卡。
- `Start using V4` 现在进入院端自己的吸乳器控制页。
- 奶量记录页支持左右奶量分别按 10 ml 增减并保存。
- 院端扩展为 16 个独立步骤，仍不读取或修改居家 Demo 状态。
- 历史进度会自动限制在当前版本的有效步骤内，避免切换或回滚版本时页面越界。

## 0.3.2 - 2026-08-19

- 恢复院端绑定成功后的 `Learn To Use` 设备教学流程。
- 院端新增教学欢迎页、详情页、最后一页和教学完成页，共 11 个独立步骤。
- 补充 Figma `833:44880` 教学最后一页原始截图和 `Finish Setup` 交互。
- 继续保持院端与居家端存储和流程完全独立。

## 0.3.1 - 2026-08-19

- 院端和居家端改为完全独立运行，各自使用单独的本地状态。
- 居家端取消院端绑定前置条件，可直接完成全部流程。
- 院端完成绑定后不再跳转或修改居家端进度。
- 统一入口移除跨端进度展示。

## 0.3.0 - 2026-08-19

- 按 Figma `755:11403`「院端使用流程」重做双端原型，不再使用上一版自拟业务界面。
- 院端串联设备空态、手动添加、发现/扫描、验证码、绑定中和绑定成功。
- 居家端串联设备教学、控制、吸乳中、吸乳记录、成功反馈和数据页。
- 使用 Figma 原始截图作为视觉层，并添加可等比缩放的无障碍交互热区。
- 保留三个访问入口和同源 `localStorage` 跨端同步。

## 0.2.0 - 2026-08-19

- 增加院端 `hospital.html` 与居家端 `home.html` 两个独立访问入口。
- 保留 `index.html` 作为双端全流程切换入口。
- 独立入口继续共享业务状态，可在两个浏览器标签之间演示跨端同步。

## 0.1.0 - 2026-08-19

- 建立院端与居家端双端可交互原型。
- 串联方案发布、设备绑定、出院交接、智能冲调、记录同步和异常预警流程。
- 增加浏览器本地状态、演示数据重置与响应式布局。
- 建立独立 Git 版本标签与回滚说明。
